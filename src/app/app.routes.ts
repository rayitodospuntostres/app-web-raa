import { Routes, RouterModule } from '@angular/router';
import { InicioComponent } from './components/principal/inicio/inicio.component'; // Asegúrate de importar tu componente
import { CategoriasComponent } from './components/principal/categorias/categorias.component';
import { NosotrosComponent } from './components/principal/nosotros/nosotros.component';

export const routes: Routes = [
  
];

export const AppRoutingModule = RouterModule.forRoot(routes);
